server:
  port: 8080

spring:
  application:
    name: jobportal

  datasource:
    url: jdbc:mysql://localhost:3306/jobportal
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

  mail:
    host: smtp.gmail.com
    port: 587
    username: kunalkumarbuj@gmail.com
    password: rgpybduwmhhaaztl     # ✅ Gmail App Password (correct)
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

  security:
    oauth2:
      client:
        registration:

          google:
            client-id: 225479004129-dp0ods82ohcaliuu90or0sjvkctmcica.apps.googleusercontent.com
            client-secret: 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
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/google"
            scope:
              - email
              - profile

          linkedin:
            client-id: 77o7ozu9cikr2a
            client-secret: WPL_AP1.z791P5dODiz50TdZ.Bp68nw==
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/linkedin"
            scope:
              - r_liteprofile
              - r_emailaddress

        provider:
          linkedin:
            authorization-uri: https://www.linkedin.com/oauth/v2/authorization
            token-uri: https://www.linkedin.com/oauth/v2/accessToken
            user-info-uri: https://api.linkedin.com/v2/me?projection=(id,localizedFirstName,localizedLastName,profilePicture(displayImage~:playableStreams))
            user-name-attribute: id

logging:
  level:
    org.springframework: INFO
    com.jobportal: DEBUG
